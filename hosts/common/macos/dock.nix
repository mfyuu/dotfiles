{ ... }:
{
  system.defaults.dock = {
    # Dock を自動的に隠す
    autohide = true;
    # 最近使ったアプリを表示しない
    show-recents = false;
    # ホバー時にアイコンを拡大する
    magnification = true;
    # アイコンサイズ (デフォルト: 48)
    tilesize = 48;
    # ホバー時の拡大サイズ
    largesize = 60;
  };
}
